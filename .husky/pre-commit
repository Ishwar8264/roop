# Purpose: Runs the production build before every local commit.
# Responsibilities: Block commits when the production build is broken.
# Notes: Husky's generated shim invokes this file with the project binaries on PATH.

pnpm run build
