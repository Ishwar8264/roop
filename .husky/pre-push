# Purpose: Runs full repository quality gates before pushing.
# Responsibilities: Block pushes when linting, types, React Doctor, or the production build fails.
# Notes: This runs the full-project gate so pushes cannot bypass staged-only checks.

pnpm run verify
