/**
 * Purpose: Create new staff page
 * Responsibility: Server wrapper that renders StaffForm in create mode
 */

import { StaffFormPage } from "./client";

export default function NewStaffPage() {
  return <StaffFormPage />;
}
